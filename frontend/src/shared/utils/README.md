# `shared/utils/` — Utility Functions

Pure helper functions with no side effects.

**Dev instructions:**
- Add date formatters, validators, API URL builders, constants here
- Each utility is a single exported function or a small module
- Must be fully testable (no mocks needed)
- Example: `formatDate(date: string): string`, `cn(...classes: string[]): string`
