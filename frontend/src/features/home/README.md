# `features/home/` — Landing Page

Public-facing home page accessible at `/`.

| Directory | Content |
|---|---|
| `pages/` | `HomePage` — hero section, CTA buttons |

**Dev instructions:**
- No auth required — public route
- Keep it lightweight: hero, how-it-works, CTA to register/login
- Style with daisyUI `hero` component + Tailwind
