# Pinned against Python 3.11 (current dev runtime).
# NOTE: Django 6.0 requires Python >=3.12 and does not exist on PyPI as 6.0.7.
# Django 5.2 is the current LTS and is what the test suite runs against.
Django==5.2.15
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.4.0
django-filter==24.3
python-dotenv==1.0.1
dnspython==2.8.0
pytest==8.3.4
pytest-django==4.9.0
